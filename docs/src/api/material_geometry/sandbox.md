# Sandbox

The `Sandbox` material geometry is composed of sand layers, ramps, and weak
boundary layers used in contractional and extensional sandbox models.

# Initialization

```@docs
ModelManager.MaterialGeometry.Sandbox.initialize!
```