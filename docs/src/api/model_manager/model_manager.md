# Model Manager

The model manager coordinates model initialization and stores the main data 
structures used by EarthBox including the model data struct [ModelData](@ref) that 
stores collections of arrays and parameters.

```@docs
ModelManager
```

```@docs
ModelManager.ModelManagerState
```