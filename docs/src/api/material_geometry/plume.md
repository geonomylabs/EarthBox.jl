# Plume

The `Plume` material geometry is used to define a circular or plume-like blob with a composition that 
differs from the surrounding mantle and elevated temperature used to simulate a mantle plume head. 

# Initialization

```@docs
ModelManager.MaterialGeometry.Plume.initialize!
```