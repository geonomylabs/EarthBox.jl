# Initializing EarthBox

EarthBox is initialized by creating a [EarthBoxState](@ref) object is composed
of a variety of data structs used by EarthBox. 


On such struct is the [ModelData](@ref) struct with pre-allocated arrays and model parameters that can be accessed

```@docs
EarthBoxState
```